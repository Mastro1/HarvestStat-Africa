{
  "name": "harveststat-africa-ui",
  "version": "1.2.0",
  "private": true,
  "description": "HarvestStat-Africa interactive data visualization UI",
  "scripts": {
    "build": "python scripts/download_data.py && cd frontend && npm run build",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "python -m backend.app"
  }
}
